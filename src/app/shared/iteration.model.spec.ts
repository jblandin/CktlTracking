import {describe, ddescribe, expect, iit, it} from '@angular/core/testing';
import {Iteration} from './iteration.model';

describe('Iteration', () => {
  it('should create an instance', () => { expect(new Iteration()).toBeTruthy(); });
});
