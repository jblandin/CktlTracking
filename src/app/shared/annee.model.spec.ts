import {describe, ddescribe, expect, iit, it} from '@angular/core/testing';
import {Annee} from './annee.model';

describe('Annee',
         () => { it('should create an instance', () => { expect(new Annee()).toBeTruthy(); }); });
