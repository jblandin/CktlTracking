import {describe, ddescribe, expect, iit, it} from '@angular/core/testing';
import {Groupe} from './groupe.model';

describe('Groupe',
         () => { it('should create an instance', () => { expect(new Groupe()).toBeTruthy(); }); });
