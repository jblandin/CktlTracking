import {describe, ddescribe, expect, iit, it} from '@angular/core/testing';
import {Declinaison} from './declinaison.model';

describe('Declinaison', () => {
  it('should create an instance', () => { expect(new Declinaison()).toBeTruthy(); });
});
